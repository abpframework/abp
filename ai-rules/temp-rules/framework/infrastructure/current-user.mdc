---
description: "ABP current user service"
globs: "**/*Service*.cs"
alwaysApply: false
---

# ABP Current User

## Using ICurrentUser
```csharp
public class MyService : ITransientDependency
{
    private readonly ICurrentUser _currentUser;

    public void DoSomething()
    {
        var userId = _currentUser.Id;
        var userName = _currentUser.UserName;
        var email = _currentUser.Email;
        var tenantId = _currentUser.TenantId;
        var isAuthenticated = _currentUser.IsAuthenticated;
        var roles = _currentUser.Roles;
    }
}
```

## In Application Services
`ApplicationService` base class has `CurrentUser` property:

```csharp
public class BookAppService : ApplicationService
{
    public async Task DoSomethingAsync()
    {
        if (CurrentUser.IsAuthenticated)
        {
            var userId = CurrentUser.Id;
        }
    }
}
```

## Getting Custom Claims
```csharp
var customValue = _currentUser.FindClaimValue("CustomClaimType");
```
