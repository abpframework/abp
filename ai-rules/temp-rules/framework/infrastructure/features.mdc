---
description: "ABP feature system for tenant/edition based feature toggling"
globs: "**/*Feature*.cs"
alwaysApply: false
---

# ABP Features

## Defining Features
```csharp
public class MyFeatureDefinitionProvider : FeatureDefinitionProvider
{
    public override void Define(IFeatureDefinitionContext context)
    {
        var myGroup = context.AddGroup("MyApp");

        myGroup.AddFeature(
            "MyApp.PdfReporting",
            defaultValue: "false",
            displayName: L("PdfReporting"),
            valueType: new ToggleStringValueType()
        );

        myGroup.AddFeature(
            "MyApp.MaxProductCount",
            defaultValue: "10",
            valueType: new FreeTextStringValueType(new NumericValueValidator(0, 1000))
        );
    }
}
```

## Checking Features
### Using Attribute
```csharp
[RequiresFeature("MyApp.PdfReporting")]
public async Task<PdfReportDto> GetPdfReportAsync()
{
    // Only runs if feature is enabled
}
```

### Using IFeatureChecker
```csharp
if (await _featureChecker.IsEnabledAsync("MyApp.PdfReporting"))
{
    // Feature is enabled
}

var maxCount = await _featureChecker.GetAsync<int>("MyApp.MaxProductCount");
```

## Feature Value Types
- `ToggleStringValueType` - Boolean toggle (checkbox)
- `FreeTextStringValueType` - Free text with validators
- `SelectionStringValueType` - Dropdown selection

## Feature Value Providers (Order)
1. `TenantFeatureValueProvider` - Per tenant
2. `EditionFeatureValueProvider` - Per edition
3. `ConfigurationFeatureValueProvider` - From configuration
4. `DefaultValueFeatureValueProvider` - Default value
