---
description: "ABP object mapping with AutoMapper"
globs: "**/*Profile*.cs,**/*Dto*.cs"
alwaysApply: false
---

# ABP Object Mapping

## AutoMapper Profile
```csharp
public class BookMapperProfile : Profile
{
    public BookMapperProfile()
    {
        CreateMap<Book, BookDto>();
        CreateMap<CreateBookDto, Book>();
    }
}
```

## Using IObjectMapper
```csharp
public class BookAppService : ApplicationService
{
    public async Task<BookDto> GetAsync(Guid id)
    {
        var book = await _bookRepository.GetAsync(id);
        return ObjectMapper.Map<Book, BookDto>(book);
    }
}
```

## Mapping Extra Properties
For extensible objects:
```csharp
CreateMap<IdentityUser, IdentityUserDto>()
    .MapExtraProperties();
```

## Best Practices
- Create profiles in `*.Application` project
- Use `CreateMap<Entity, Dto>().ReverseMap()` for bidirectional mapping
- Handle extra properties for extensible entities
