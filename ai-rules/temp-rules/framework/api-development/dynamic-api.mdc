---
description: "ABP dynamic API client proxies"
globs: "**/*HttpApiClient*"
alwaysApply: false
---

# ABP Dynamic API Client Proxies

## Server-Side (Auto Controllers)
Application services are automatically exposed as HTTP APIs.

## Client-Side Proxy Generation
Generate C# client proxies:
```bash
abp generate-proxy -t csharp -u https://localhost:44300
```

## Using Client Proxies
```csharp
public class MyService : ITransientDependency
{
    private readonly IBookAppService _bookAppService;

    public MyService(IBookAppService bookAppService)
    {
        _bookAppService = bookAppService;
    }

    public async Task<List<BookDto>> GetBooksAsync()
    {
        return await _bookAppService.GetListAsync(new GetBookListDto());
    }
}
```

## Configuration
```csharp
Configure<AbpRemoteServiceOptions>(options =>
{
    options.RemoteServices.Default = new RemoteServiceConfiguration("https://localhost:44300");
});
```

## HttpApi.Client Module
```csharp
[DependsOn(typeof(BookStoreHttpApiClientModule))]
public class MyClientModule : AbpModule
{
}
```
