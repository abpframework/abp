---
description: "ABP repository pattern and conventions"
globs: "**/*Repository*.cs,**/Repositories/**/*.cs"
alwaysApply: false
---

# ABP Repositories

## Repository Interface (Domain Layer)
```csharp
public interface IBookRepository : IRepository<Book, Guid>
{
    Task<Book> FindByNameAsync(string name, CancellationToken cancellationToken = default);
    
    Task<List<Book>> GetListByAuthorAsync(
        Guid authorId,
        bool includeDetails = false,
        CancellationToken cancellationToken = default);
}
```

## Repository Implementation (EF Core)
```csharp
public class BookRepository : EfCoreRepository<MyDbContext, Book, Guid>, IBookRepository
{
    public BookRepository(IDbContextProvider<MyDbContext> dbContextProvider)
        : base(dbContextProvider)
    {
    }

    public async Task<Book> FindByNameAsync(string name, CancellationToken cancellationToken = default)
    {
        return await (await GetDbSetAsync())
            .FirstOrDefaultAsync(b => b.Name == name, GetCancellationToken(cancellationToken));
    }

    public async Task<List<Book>> GetListByAuthorAsync(
        Guid authorId,
        bool includeDetails = false,
        CancellationToken cancellationToken = default)
    {
        return await (await GetDbSetAsync())
            .IncludeDetails(includeDetails)
            .Where(b => b.AuthorId == authorId)
            .ToListAsync(GetCancellationToken(cancellationToken));
    }
}
```

## Best Practices
- One repository per aggregate root only
- Inherit from `IBasicRepository<T, TKey>` for public interfaces (no `IQueryable`)
- All methods async with `CancellationToken cancellationToken = default`
- Single entity methods: `includeDetails = true`
- List methods: `includeDetails = false`
- Use `GetCancellationToken(cancellationToken)` in implementations
- Don't return projection classes like `BookWithAuthor`
