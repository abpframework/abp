@using System;
@using Volo.Abp.Identity
@using Microsoft.Extensions.Localization
@using Volo.Abp.Identity.Localization

@inject IStringLocalizer<IdentityResource> L

@(Data.As<IdentityRoleDto>().Name)
@if (Data.As<IdentityRoleDto>().IsDefault)
{
    <MudChip T="string" Color="Color.Success" Size="Size.Small" Class="ms-2">@L["DisplayName:IsDefault"]</MudChip>
}
@if (Data.As<IdentityRoleDto>().IsPublic)
{
    <MudChip T="string" Color="Color.Info" Size="Size.Small" Class="ms-2">@L["DisplayName:IsPublic"]</MudChip>
}
